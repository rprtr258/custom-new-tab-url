{
  "name": "custom-new-tab-url",
  "version": "1.1.5",
  "private": true,
  "scripts": {
    "build": "bun build --check src/newtab.ts src/options.ts src/background.ts --outdir js",
    "typecheck": "tsc --noEmit",
    "watch": "bun build --check src/newtab.ts src/options.ts --outdir js --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "typescript": "^6.0.3"
  }
}
